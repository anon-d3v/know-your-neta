# ============================================================================
# Supabase Configuration
# ============================================================================

# Your Supabase project URL
# Find this in: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co

# Service role key (for admin operations like imports)
# WARNING: Keep this secret! Never commit to Git or share publicly
# Find this in: Supabase Dashboard → Project Settings → API → service_role key
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Public anon key (for client-side app)
# This is safe to expose in your app
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# MPLADS Automation Configuration (Optional)
# ============================================================================

# Directory for CSV files (default: D:\KYN on Windows, /tmp/mplads-csvs on Linux)
# Uncomment and modify if you want to use a different location
# MPLADS_CSV_DIR=D:\KYN

# Webhook URL for notifications (Discord, Slack, etc.)
# Get Discord webhook: Server Settings → Integrations → Webhooks → New Webhook
# Get Slack webhook: Slack App Settings → Incoming Webhooks
# MPLADS_NOTIFY_WEBHOOK=https://discord.com/api/webhooks/your-webhook-here

# Maximum age of CSV files in days before re-downloading (default: 7)
# MPLADS_MAX_AGE_DAYS=7

# ============================================================================
# Development (Optional)
# ============================================================================

# Set to development for additional logging
# NODE_ENV=development

# ============================================================================
# Notes
# ============================================================================

# 1. Copy this file to .env in the same directory
# 2. Fill in your actual values
# 3. Never commit the .env file to Git (it's in .gitignore)
# 4. Keep your service key secure - it has full database access

# To get your Supabase credentials:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings → API
# 4. Copy the URL and keys

# For GitHub Actions:
# Add these as repository secrets instead of using .env:
# Settings → Secrets and variables → Actions → New repository secret
# - SUPABASE_URL
# - SUPABASE_SERVICE_KEY
# - DISCORD_WEBHOOK (optional)
